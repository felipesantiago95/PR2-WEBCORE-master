@model List<PR2_WEBCORE.Models.Aforo>

@{
    ViewData["Title"] = "Aforos";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<head>
    <meta name="viewport" content="width=device-width" />
    <title>Aforo</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" />
</head>
<div class="container mt-5">
    <h1>Lista de Aforos</h1>
    <p>
        <a asp-action="Create" class="btn btn-primary">Crear Nuevo</a>
    </p>
    <table class="table table-striped">
        <thead>
            <tr>
                <th>ID</th>
                <th>Descripción</th>
                <th>Porcentaje</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.IdAforo</td>
                    <td>@item.Descripcion</td>
                    <td>@item.Porcentaje</td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.IdAforo" class="btn btn-warning">Editar</a> |
                    <a asp-action="Details" asp-route-id="@item.IdAforo" class="btn btn-info">Detalles</a> |
                        <a asp-action="Delete" asp-route-id="@item.IdAforo" class="btn btn-danger">Eliminar</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
